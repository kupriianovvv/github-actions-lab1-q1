name: CI

on:
  push:  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: 3.11

    - name: 'clone repo'
      run: 'git clone https://github.com/kupriianovvv/github-actions-lab1-q1.git'

    - name: 'change dir'
      run: 'cd github-actions-lab1-q1'
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
